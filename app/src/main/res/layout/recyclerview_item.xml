<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical">

    <android.support.v7.widget.CardView
        android:id="@+id/card_view"
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="5dp"
        app:cardCornerRadius="2dp"
        app:cardElevation="2dp"
        app:cardPreventCornerOverlap="false"
        app:contentPadding="0dp">

	    <LinearLayout
		    xmlns:android="http://schemas.android.com/apk/res/android"
		    android:orientation="horizontal"
		    android:layout_width="fill_parent"
		    android:layout_height="wrap_content"
		    android:background="@color/background_material_light">

		    <ImageView
			    android:id="@+id/image"
			    android:src="@drawable/abc_btn_radio_material"
			    android:padding="5px"
			    android:layout_width="@dimen/profile_image_icon_width"
			    android:layout_height="@dimen/profile_image_icon_height"/>

		    <LinearLayout
			    android:layout_width="0dp"
			    android:layout_height="fill_parent"
			    android:layout_weight="1"
			    android:orientation="vertical">

			    <TextView
				    android:id="@+id/name"
				    android:hint="Tony Stark"
				    android:layout_weight="3"
				    android:textSize="20sp"
				    android:padding="10px"
				    android:textColor="#000000"
				    android:layout_width="fill_parent"
				    android:layout_height="0dp"
				    android:typeface="sans"/>

			    <TextView
				    android:id="@+id/actual_price"
				    android:text="₹ something"
				    android:layout_weight="1"
				    android:textSize="10sp"
				    android:layout_width="fill_parent"
				    android:layout_height="0dp"/>
		    </LinearLayout>

<!--
		    <Switch
			    android:id="@+id/favoriteToggle"
			    android:layout_width="wrap_content"
			    android:layout_height="match_parent"
			    android:checked="false"
			    android:padding="@dimen/access_button_padding_all"/>
		    -->
	    </LinearLayout>
    </android.support.v7.widget.CardView>


</RelativeLayout>